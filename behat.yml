default:
  paths:
    features: ./tests/features
    bootstrap: %behat.paths.features%/bootstrap

  formatter:
      name: progress,junit,html
      parameters:
        output_path: null,./build/logs/behat,./build/behat/index.html